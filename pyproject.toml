[project]
name = "hermes-unsafe-autonomy"
version = "0.1.1"
description = "A local, recoverable Hermes Agent autonomy reference substrate"
requires-python = ">=3.11"
dependencies = []

[tool.hermes-vault]
provider_name = "hermes_vault"
schema_version = 1
